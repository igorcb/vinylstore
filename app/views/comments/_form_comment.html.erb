<%= form_for(@comment, html: {multipart: true	}) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
 
  <%= hidden_field_tag "comment[disc_id]", @disc.id %>

  <%= render 'comments/fields', f: f %>
  
  <%= f.submit "Enviar comentario", class: "btn btn-success" %>
<% end %>
